import { z } from 'zod';
export const registerSchema=z.object({firstName:z.string().min(1).max(50),lastName:z.string().min(1).max(50),username:z.string().regex(/^[a-zA-Z0-9_]{3,30}$/),email:z.string().email(),password:z.string().min(8),city:z.string().min(2)});
export const loginSchema=z.object({email:z.string().email(),password:z.string().min(1)});
export const productSchema=z.object({quantity:z.number().int().positive(),productName:z.string().min(2).max(150),productUrl:z.string().url(),unitPrice:z.number().nonnegative().optional(),variant:z.string().max(100).optional(),size:z.string().max(50).optional(),color:z.string().max(50).optional(),notes:z.string().max(500).optional()});
export const createGroupSchema=productSchema.extend({promotionId:z.string(),title:z.string().min(3).max(100),city:z.string().min(2),deliveryMethod:z.enum(['MEETUP','SINGLE_ADDRESS','INDIVIDUAL_DELIVERY','UNDECIDED']).optional(),meetupArea:z.string().max(100).optional()});
export const promotionSchema=z.object({storeId:z.string(),title:z.string().min(3),description:z.string().min(10),promotionType:z.enum(['BUY_X_GET_DISCOUNT','BUY_X_GET_Y_FREE','PERCENTAGE_DISCOUNT','FIXED_DISCOUNT','MINIMUM_SPEND','OTHER']),requiredQuantity:z.number().int().positive(),discountPercentage:z.number().min(0).max(100).optional(),discountAmount:z.number().nonnegative().optional(),freeItemsQuantity:z.number().int().positive().optional(),minimumSpend:z.number().nonnegative().optional(),promoCode:z.string().optional(),promotionUrl:z.string().url(),imageUrl:z.string().url().optional(),category:z.string(),city:z.string().optional(),startsAt:z.coerce.date().optional(),expiresAt:z.coerce.date()});
