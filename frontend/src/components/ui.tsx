import{LoaderCircle,Star}from'lucide-react';import type{ReactNode}from'react';export const Loading=()=> <div className="flex min-h-48 items-center justify-center"><LoaderCircle className="animate-spin text-brand-600"/></div>;export const Empty=({title,text}:{title:string;text:string})=><div className="card p-10 text-center"><h3 className="text-xl">{title}</h3><p className="mt-2 text-slate-500">{text}</p></div>;export const ErrorBox=({message}:{message:string})=><div className="rounded-xl bg-red-50 p-4 text-red-700">{message}</div>;export const Badge=({children,tone='green'}:{children:ReactNode;tone?:'green'|'gray'|'amber'|'blue'})=><span className={`badge ${tone==='green'?'bg-brand-50 text-brand-700':tone==='amber'?'bg-amber-100 text-amber-800':tone==='blue'?'bg-blue-100 text-blue-800':'bg-slate-100 text-slate-700'}`}>{children}</span>;export const Rating=({value,count}:{value:number;count?:number})=><span className="inline-flex items-center gap-1 text-sm"><Star className="h-4 w-4 fill-amber-400 text-amber-400"/>{value.toFixed(1)}{count!==undefined&&<span className="text-slate-400">({count})</span>}</span>;export const Progress=({value,max}:{value:number;max:number})=><div><div className="mb-1 flex justify-between text-sm font-medium"><span>{value} of {max} items</span><span>{Math.round(value/max*100)}%</span></div><div className="h-2.5 overflow-hidden rounded-full bg-slate-100"><div className="h-full rounded-full bg-brand-500" style={{width:`${Math.min(100,value/max*100)}%`}}/></div></div>;
